{
  "name": "muse",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Live music coding DSL: Monaco → TypeScript DSL/REPL → Pattern IR → WASM (Rust clock/scheduler/DSP) → AudioWorklet",
  "scripts": {
    "build:wasm": "bash scripts/build-wasm.sh",
    "dev": "npm run build:wasm && vite",
    "build": "npm run build:wasm && tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "cargo test --quiet && npm run build:wasm && vitest run",
    "typecheck": "tsc --noEmit",
    "e2e": "node scripts/e2e.mjs",
    "verify": "npm test && npm run cli:test && tsc --noEmit && vite build && node scripts/e2e.mjs",
    "build:cli": "bash scripts/build-cli.sh",
    "cli": "bash scripts/build-cli.sh && node dist/cli/muse.cjs",
    "cli:test": "bash scripts/build-cli.sh && node scripts/cli-test.mjs"
  },
  "devDependencies": {
    "@types/node": "^26.6.2",
    "esbuild": "^0.28.2",
    "monaco-editor": "^0.52.2",
    "playwright": "^1.63.0",
    "typescript": "^5.7.2",
    "vite": "^6.0.7",
    "vitest": "^3.0.5"
  },
  "bin": {
    "muse": "dist/cli/muse.cjs"
  },
  "dependencies": {
    "acorn": "^8.18.0"
  }
}
